# -*- coding: utf-8 -*-
# image_maker_app/lib/explanation/exp_image_usage.py
# ============================================================
# 利用履歴ページ 説明UI
#
# 機能：
# - ページ上部の説明カードを描画する
# - 詳細説明 expander を描画する
# - 月別集計 / モデル別集計 / CSV出力を説明する
# ============================================================

from __future__ import annotations

# ============================================================
# imports
# ============================================================
from typing import Any

import streamlit as st

from common_lib.ui.help_expander import render_themed_help_expander
from common_lib.ui.intro_panel import (
    render_info_card_compact,
    render_info_card_bullets_compact_custom,
)


# ============================================================
# ページ上部説明UI
# ============================================================
def render_image_usage_page_intro() -> None:
    # ------------------------------------------------------------
    # AI利用
    # ------------------------------------------------------------
    render_info_card_compact(
        body_html="""
🟢 このページでは，<b>AIは使用しません</b>．
"""
    )

    # ------------------------------------------------------------
    # ページ概要
    # ------------------------------------------------------------
    render_info_card_compact(
        body_html="""
このページでは，自分の画像生成・画像修正の利用履歴を確認できます．
月別，モデル別に集計し，CSVとしてダウンロードできます．
"""
    )

    # ------------------------------------------------------------
    # 使い方
    # ------------------------------------------------------------
    render_info_card_bullets_compact_custom(
        title="使い方",
        items=[
("", "月別・モデル別に画像生成・画像修正の件数を確認できます．"),
("", "左側（サイドバー）で対象年月を選択できます．また，必要に応じて開始日・終了日で絞り込むこともできます．"),
("", "⚠️ このページに表示されるのは，自分の利用履歴のみです．"),
        ],
    )

    st.markdown(
        "<div style='height:16px'></div>",
        unsafe_allow_html=True,
    )


# ============================================================
# public API：詳細説明 expander
# ============================================================
def render_image_usage_help_expander(
    *,
    theme: dict[str, Any] | None = None,
    banner_key: str = "pink_soft",
) -> None:
    render_themed_help_expander(
        expander_key=HELP_EXPANDER_KEY,
        expander_title=HELP_EXPANDER_TITLE,
        tabs=HELP_TABS,
        theme=theme,
        banner_key=banner_key,
        expanded=False,
    )


# ============================================================
# expander 設定
# ============================================================
HELP_EXPANDER_KEY = "image_usage_help_expander"
HELP_EXPANDER_TITLE = "詳細説明（クリックで展開）"


# ============================================================
# 詳細説明：利用履歴
# ============================================================
USAGE_TEXT = """

##### 1. このページで確認できること

このページでは，ログイン中ユーザー本人の画像生成・画像修正ログを確認できます。

主に次の情報を確認できます。

- 画像生成件数
- 画像修正件数
- 月別件数
- 月別・モデル別件数
- 利用ログの対象期間

---

##### 2. 表示対象

このページでは，ログイン中の自分のログだけを表示します。  
他のユーザーのログは表示されません。

---

##### 3. 対象年月の選択

左側（サイドバー）で対象年月を選択します。  
複数月を選択できます。

ログが多い場合は，必要な月だけを選択してください。

</div>
"""


# ============================================================
# 詳細説明：集計表
# ============================================================
SUMMARY_TEXT = """

##### 1. 月別集計

月別集計では，月ごとに次の件数を表示します。

- 画像生成
- 画像修正
- 総数

---

##### 2. 月別・モデル別集計

月別・モデル別集計では，使用したモデルごとに件数を確認できます。

例：

- OpenAI / gpt-image-1
- Gemini / gemini-2.5-flash-image

モデル別に確認することで，どのモデルをどの程度利用したかを把握できます。

---

##### 3. 件数の意味

このページの件数は，ログに記録された実行回数です。  
画像生成または画像修正ボタンを押して，処理が記録されたものが集計対象になります。

</div>
"""


# ============================================================
# 詳細説明：CSV
# ============================================================
CSV_TEXT = """

##### 1. CSVダウンロード

各集計表は CSV としてダウンロードできます。

CSV は Excel などで開ける形式です。  
日本語列名で出力されます。

---

##### 2. 利用例

- 月次利用状況の確認
- モデル別の利用状況確認
- 利用件数の報告
- 概算料金の確認資料作成

</div>
"""


# ============================================================
# 詳細説明：FAQ
# ============================================================
FAQ_TEXT = """

##### よくある質問（FAQ）


###### Q1. モデル別に確認できますか？

はい。  
OpenAI / Gemini など，ログに記録された provider / model ごとに集計できます。

---

###### Q2. ログが表示されない場合は？

対象年月が未選択，または対象期間にログがない可能性があります。  
サイドバーで対象年月を確認してください。

</div>
"""


# ============================================================
# 詳細説明タブ
# ============================================================
HELP_TABS = [
    ("利用履歴", USAGE_TEXT),
    ("集計表", SUMMARY_TEXT),
    ("CSV出力", CSV_TEXT),
    ("FAQ", FAQ_TEXT),
]